<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CircleOnApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CircleOn">
        <activity
            android:name=".view.screen.IntroActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.screen.auth.ChangePasswordActivity"
            android:exported="false" />
        <activity
            android:name=".view.screen.auth.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".view.screen.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".view.screen.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan">
        </activity>
    </application>

</manifest>
